---
title: "mouse1release"
description: "Simulates a left mouse button release."
---

```lua
mouse1release(x: number?, y: number?): void
```
Simulates a left mouse button release **only** if the roblox window is focused.

If `x` and `y` paramaters aren't empty, the mouse will move and release there.

