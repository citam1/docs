---
title: "decrypt"
description: "Returns an decrypted string based on the provided input, key and the IV."
---

```lua
decrypt(input: string, key: string, iv: string , mode: string): string, string
```
Decrypts the encrypted input and returns the original string.
