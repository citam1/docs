---
title: "mouse2release"
description: "Simulates a right mouse button release."
---

```lua
mouse2release(x: number?, y: number?): void
```
Simulates a right mouse button release **only** if the roblox window is focused.

If `x` and `y` paramaters aren't empty, the mouse will move and release there.

